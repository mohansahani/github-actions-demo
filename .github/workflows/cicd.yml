name: CICD 

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        run: echo "Code clone from Github"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: container build
        run: echo "Container building done"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: code testing
        run: echo "Testing the code"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deployment of code
        run: echo "Deployment success"
